# dotfiles

## Install Developer Tools

## Commands

| Command         | Description |
| :-------------- | :---------- |
| `make git`      | Configures git with my preferred defaults |
| `make shell`    | |
| `make homebrew` | Installs homebrew and all associated libraries and applications from the base `Brewfile` and `Local.brewfile` (which is excluded from the repository). |
| `make ruby`     | |
| `make vscode`   | |
